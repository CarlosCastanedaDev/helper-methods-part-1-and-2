<h1>Details for <%= @director.name %></h1>

<h3>Bio</h3>
<%= @director.bio %>

<h4>DOB</h4>
<%= @director.dob %>

<div>

<%= link_to "Edit information" , edit_director_path(@director)%>
</div>

<div>
  <%= link_to "Delete director",  @director, data: { turbo_method: :delete}%>
</div>
